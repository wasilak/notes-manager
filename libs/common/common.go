package common

import (
	"context"
)

var CTX context.Context

func GetAppVersion() string {
	// Implement the logic for getting the app version from package.json
	// You will need to read the version from the package.json file in Go
	return "" // Replace this with the actual app version
}
